#Instructions
#We've provided the lists row_2 and empty_row in the code editor.
#Use the len() function to find the length of row_2, and store the result in a variable named row_2_length.
#Use the len() function to find the length of empty_row, and store the result in a variable named empty_row_length.

row_2 = ['Instagram', 0.0, 'USD', 2161558, 4.5]
empty_row = []
row_2_length = len(row_2)
empty_row_length = len(empty_row)
